# dbt — build artifacts (also gitignored inside dbt_scaffold/, repeated here
# so dbt commands accidentally run from the repo root don't leak artifacts)
target/
dbt_packages/
logs/
.user.yml

# Local secrets / env
.env
.env.*
!.env.example
*.pem
*.p8

# Python (utility scripts, ad-hoc venvs)
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
